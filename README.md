# bandit
I created a bash script to automate the login to the OTW(Over The Wire) bandit challange and also created a bash script which automatically saves the passwords in the text file in required format so that it can be used to login using sshpass
